# wirespec-driver

Compilation driver and CLI for [wirespec](https://github.com/wirespec-lang/wirespec).

Orchestrates the full compilation pipeline: module resolution, parsing, semantic analysis, layout, codec IR, and backend code generation.

This is an internal crate used by the wirespec compiler. For the user-facing tool, see [`wirespec`](https://crates.io/crates/wirespec).
